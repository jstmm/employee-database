# Employee Database

To build:

```bash
$ ./configure.sh
$ make
```

To run:

```bash
$ ./bin/dbview
```

Options (for testing getopt for now):

> -n              # set newfile to true
>
> -f <filepath>   # specify a file path
